version: "3.9"

services:
  chatterbox:
    build: .
    container_name: chatterbox
    ports:
      - "5102:5102"
    volumes:
      - /c/DEV/ReadAloud/ChatterBox/audio_prompts:/app/audio_prompts
    restart: unless-stopped
